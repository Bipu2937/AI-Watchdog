# AI-Watchdog
The AI-Watchdog Enforcer is a "Security Gate" component designed to sit between the user authentication and the application dashboard. Unlike standard loading spinners, this project simulates a Runtime Protection sequence common in high-security EDR (Endpoint Detection and Response) suites.
